{
  "version": 2,
  "builds": [
    { "src": "**/*", "use": "@vercel/static",
      "config": { "excludeFiles": "app.py,requirements.txt,site_data.db,*.pyc" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
